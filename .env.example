# Used only for local development WITHOUT Docker (npm run start:dev / npm run dev).
# When running via `docker compose up`, ignore this file — all env vars are
# already set with working defaults inside docker-compose.yml.

DATABASE_URL=postgresql://friendmap:friendmap@localhost:5432/friendmap
JWT_SECRET=replace-with-a-long-random-secret
REDIS_HOST=localhost
REDIS_PORT=6379
PORT=3000
VITE_API_URL=http://localhost:3000
